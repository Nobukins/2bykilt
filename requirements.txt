###############################################
# 2bykilt Root Requirements (Delegation Stub) #
###############################################

# このファイルは直接ピンを列挙しません。
# 再現可能インストールは pip-tools により生成された
# constraints/requirements.txt を使用してください。

# Usage (local dev):
#   python -m venv venv
#   source venv/bin/activate
#   pip install -r constraints/requirements.txt
#   playwright install

# Top-level dependencies を変更したい場合:
#   1. requirements.in を編集
#   2. pip-compile --output-file constraints/requirements.txt requirements.in
#   3. 変更をコミット & CI で確認

# Why:
#   - 手動ピン連鎖による競合を防ぐ
#   - CI / Sonar / SAST で同一ロックを保証
#   - 差分レビュー容易化

# Minimal mode:
#   pip install -r requirements-minimal.txt

# 実際の依存は constraints/requirements.txt を参照
-r constraints/requirements.txt