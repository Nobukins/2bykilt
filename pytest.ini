[pytest]
markers =
    ci_safe: safe to run in CI (no external browsers required)
    local_only: requires local environment (browsers / GUI)
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
asyncio_default_test_loop_scope = function
